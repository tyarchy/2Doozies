const express = require('express');
const router = express.Router();
const db = require('./config/connection.js');

// // Display all to-dos
// router.get('/api/doozies', (req, res) => {

// })

// Create new to-do

// Update to-do

// Delete to-do

module.exports = router;